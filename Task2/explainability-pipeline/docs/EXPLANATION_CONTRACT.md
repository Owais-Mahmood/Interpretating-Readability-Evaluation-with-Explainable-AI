# Explanation contract

Each explanation records the example, method, target, units, scores, unit type,
sign semantics, parameters, runtime, and diagnostics. Store raw and aligned
forms separately. Never discard raw scores.
