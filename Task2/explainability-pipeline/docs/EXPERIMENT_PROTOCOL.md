# Experiment protocol

Freeze the dataset, split, evaluation cohort, checkpoint, target score, seeds,
method parameters, alignment, metric definitions, and tests before running.

All methods must use identical examples, checkpoint, targets, score scale,
comparison units, and perturbation policy where applicable.
